{
  "name": "nifty-ink",
  "version": "1.0.0",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "yarn prepare && graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ azf20/nifty-ink",
    "create-local": "graph create --node http://localhost:8020/ azf20/nifty-ink",
    "remove-local": "graph remove --node http://localhost:8020/ azf20/nifty-ink",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 azf20/nifty-ink",
    "prepare": "mustache ./config/config.json ./src/subgraph.template.yaml > ./subgraph.yaml"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.18.0",
    "@graphprotocol/graph-ts": "0.18.0",
    "mustache": "^4.1.0"
  }
}
